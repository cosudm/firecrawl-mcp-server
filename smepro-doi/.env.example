# Copy to .env (or export in your shell) to enable AI extraction.
# The key is read ONLY by server/api.mjs — never shipped to the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# SMEPRO_MODEL=claude-opus-4-8
# PORT=8787
